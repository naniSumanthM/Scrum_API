let userStories = [];

let story = {
  title: "Test story",
  description: "This is a test description",
  status: "defined"
};

userStories.push(story);

userStories.forEach(story => {
  console.log(story);
});

//via http make a post request to ur api route, that inserts/posts to the mongo server
//make a mongo server
//jff insert something
//take that code and insert something else
